<div class="col col-2">
    <div class="card h-75">
        <img style="height: 80%" class="card-img-top" 
        src="{{ asset('photos/' ~ nom_dossier|url_encode ~ '/' ~ d.filename) }}" 
        alt="{{ d.filename }}">
        <div class="card-body">
            <h5 class="card-title">{{ d.filename }}</h5>
        </div>
    </div>
</div>